export function OnePack(key: number, name: string) {}

export default function Package() {
  const packageList = ["Serenity", "Serenity", "Experience"];
  return (
    <>
      {/* {packageList.map((name, index) => (
        // <OnePack key={index} name={name}></OnePack>
      ))} */}
    </>
  );
}
