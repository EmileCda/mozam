import { StrictMode } from "react";
import { FooterContainer } from "../style/Footer.style";
/**
 * this component is in charge to display the footer
 * footer contains : logo, nav-bar,CGV, term&condition
 *
 */

export default function Footer() {
  return (    <>
      <FooterContainer>
      </FooterContainer>
    </>
  );
}
